# stunning-spork

## Command to convert to puml to svg
```bash
 docker run -v $(pwd):/data plantuml/plantuml -tsvg Autogen-grouped.puml
```

```bash
 docker run -v $(pwd):/data plantuml/plantuml -tsvg Autogen-original.puml
```